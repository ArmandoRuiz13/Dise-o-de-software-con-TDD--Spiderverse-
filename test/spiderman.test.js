const { TestWatcher } = require("jest")

describe('Prueba Unitaria para agregar datos para un spiderman a la clase spiderman', () =>{
    Test('Prueba 1 Spiderman', () => {

        const result = 1+2
        expect(result).toBe(18)
    })
})